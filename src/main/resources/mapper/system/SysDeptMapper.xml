<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.rtt.server.system.mapper.SysDeptMapper">

    <resultMap id="DeptResult" type="cn.rtt.server.system.domain.entity.SysDept">
        <id property="deptId" column="dept_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="ancestors" column="ancestors"/>
        <result property="deptName" column="dept_name"/>
        <result property="orderNum" column="order_num"/>
        <result property="leaderId" column="leader_id"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="leader" column="leader"/>
        <result property="roleIds" column="role_ids"/>
        <result property="gmtCreate" column="gmt_create"/>
        <result property="gmtModified" column="gmt_modified"/>
    </resultMap>

    <select id="search" resultMap="DeptResult">
        select
        t1.dept_id,
        t1.parent_id,
        t1.ancestors,
        t1.dept_name,
        t1.order_num,
        t1.leader_id,
        t1.phone,
        t1.email,
        t1.status,
        t1.remark,
        t1.role_ids,
        t1.gmt_create,
        t1.gmt_modified,
        t2.username as leader
        from sys_dept t1
        left join sys_user t2 on t1.leader_id = t2.user_id
        where
        t1.status != -1
        <if test="request.deptName != null and request.deptName != ''">
            and t1.deptName like concat('%', #{request.deptName}, '%')
        </if>
        <if test="request.status != null">
            and t1.status = #{request.status}
        </if>
    </select>

</mapper>
